---
import {css} from "../../../styled-system/css";

const {iconSrc} = Astro.props;

---
<img src={iconSrc}
     alt="representation d'un lien"
     class={css({
         height: "12vh",
         width: "12vh",
         marginBottom: "4%"
     })}
/>