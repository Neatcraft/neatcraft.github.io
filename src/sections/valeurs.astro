---
import { css } from "../../styled-system/css";
import { title } from "../../styled-system/patterns";
import SectionTitle from "../components/sectionTitle.astro";
import SubTitle from "../components/subtitle.astro";
import ParagraphEnd from "../components/card/ParagraphEnd.astro";
import ParagraphStart from "../components/card/ParagraphStart.astro";
---

<section id="engagement" class={css({
    display: "flex",
    flexDirection: "column",
    padding: "3% 2% 5% 2%",
    justifyContent: "center",

    width: "100%",
    height: "100%",
    bgColor: "third.100",
    bgImage: "url(/img/triangle.svg)",
})}>
    <SectionTitle iconSrc="/img/icons/museum.svg" titleClass={title({ color: "white" })}>
        valeurs
    </SectionTitle>

    <div class={css({
        display: "flex",
        flexDirection: "column",
        width: "100%",
        height: "auto",
        padding: "5%",
        "@media (min-width: 1024px)": {
            display: "grid",
            gridTemplateColumns: "repeat(4, 1fr)", // 4 colonnes
            gridTemplateRows: "repeat(2, auto)",  // 2 lignes
            gap: "1rem",
        },
    })}>

        <!-- Bloc pour Rigueur -->
        <div class={css({
            display: "flex",
            flexDirection: "column",
            justifyContent: "center",
            alignItems:"end",
            alignSelf: "baseline",
            color: "white",
            "@media (min-width: 1024px)": {
                gridColumn: "1 / 2",
                gridRow: "1 / 2",
            },
        })}>
            <!-- Icône Compas en dessous de Rigueur -->
            <div class={css({
                display: "flex",
                justifyContent: "center",
                alignItems: "center",

            })}>
                <img src="/img/icons/compas.svg" alt="Compas" class={css({
                    width: "50px", // Taille réduite
                    height: "50px", // Taille réduite
                    marginBottom: "1em",
                    "@media (min-width: 1024px)": {
                        display: "none", // Masque l'icône en plus grand
                    },
                })} />
            </div>
            <SubTitle>Rigueur</SubTitle>
            <ParagraphEnd class={css({ color: "white" })}>
                Nous appliquons une rigueur sans compromis à chaque étape de notre travail pour garantir des résultats exceptionnels.
            </ParagraphEnd>
        </div>

        <!-- Icône Compas dans un cercle (masquée sous 1024px) -->
        <div class={css({
            display: "none", // Masque le cercle en dessous de 1024px
            justifyContent: "center",
            alignItems: "center",
            zIndex: "1",

            "@media (min-width: 1024px)": {
                display: "flex",
                gridColumn: "2 / 2",
                gridRow: "2 / 1",
            },
        })}>
            <div class={css({
                borderRadius: "50%",
                backgroundColor: "white",
                width: "250px",
                height: "250px",
                display: "flex",
                justifyContent: "center",
                alignItems: "center",
                border: "5px solid secondary.100",
            })}>
                <div class={css({
                    borderRadius: "50%",
                    width: "240px",
                    height: "240px",
                    border: "5px solid #ed7d1a",
                    display: "flex",
                    justifyContent: "center",
                    alignItems: "center",
                })}>
                    <img src="/img/icons/compas.svg" alt="Compas" class={css({
                        width: "100px",
                        height: "100px",
                    })} />
                </div>
            </div>
        </div>

        <!-- Cercle central -->
        <div class={css({
            display: "none", // Masque le cercle central en dessous de 1024px
            justifyContent: "center",
            alignItems: "center",
            "@media (min-width: 1024px)": {
                display: "flex",
                gridColumn: "2 / 4",   // Colonne 2 à 3
                gridRow: "1 / 3",      // Ligne 1 à 2
            },
        })}>
            <img src="/img/icons/circle.svg" alt="Cercle central" class={css({
                width: "450px",
                height: "450px",
            })} />
        </div>

        <!-- Bloc pour Empathie -->
        <div class={css({
            display: "flex",
            flexDirection: "column",
            justifyContent: "center",
            alignSelf: "baseline",
            alignItems: "end",
            color: "white",
            "@media (min-width: 1024px)": {
                gridColumn: "1 / 2",
                gridRow: "2 / 3",
            },
        })}>
            <!-- Icône Speaker en dessous d'Empathie -->
            <div class={css({
                display: "flex",
                justifyContent: "center",
                alignItems: "center",
                "@media (max-width: 1024px)": {
                    marginBottom: "1rem", // Espacement entre l'icône et le sous-titre
                },
            })}>
                <img src="/img/icons/speaker.svg" alt="Speaker" class={css({
                    width: "50px", // Taille réduite
                    height: "50px", // Taille réduite
                    "@media (min-width: 1024px)": {
                        display: "none", // Masque l'icône en plus grand
                    },
                })} />
            </div>
            <SubTitle>Empathie</SubTitle>
            <ParagraphEnd class={css({ color: "white" })}>
                Nous serons toujours à votre écoute pour vous offrir le produit le plus adapté à vos besoins.
            </ParagraphEnd>
        </div>

        <!-- Icône Speaker dans un cercle (masquée sous 1024px) -->
        <div class={css({
            display: "none", // Masque le cercle en dessous de 1024px
            justifyContent: "center",
            alignItems: "center",
            "@media (min-width: 1024px)": {
                display: "flex",
                gridColumn: "2 / 3",
                gridRow: "2 / 3",
            },
        })}>
            <div class={css({
                borderRadius: "50%",
                backgroundColor: "white",
                width: "250px",
                height: "250px",
                display: "flex",
                justifyContent: "center",
                alignItems: "center",
                border: "5px solid secondary.100",
            })}>
                <div class={css({
                    borderRadius: "50%",
                    width: "240px",
                    height: "240px",
                    border: "5px solid #ed7d1a",
                    display: "flex",
                    justifyContent: "center",
                    alignItems: "center",
                })}>
                    <img src="/img/icons/speaker.svg" alt="Speaker" class={css({
                        width: "100px",
                        height: "100px",
                    })} />
                </div>
            </div>
        </div>

        <!-- Bloc pour Créativité -->
        <div class={css({
            display: "flex",
            flexDirection: "column",
            justifyContent: "center",
            color: "white",
            "@media (min-width: 1024px)": {
                gridColumn: "4 / 4", // Positionne le bloc dans la colonne 3
                gridRow: "1 / 2",     // Positionne le bloc dans la ligne 1
            },
        })}>
            <!-- Icône dans le bloc Créativité -->
            <div class={css({
                display: "flex",
                justifyContent: "center",
                alignItems: "center",
                "@media (max-width: 1024px)": {
                    marginBottom: "1rem", // Espacement entre l'icône et le sous-titre
                },
            })}>
                <img src="/img/icons/ampoule.svg" alt="Ampoule" class={css({
                    width: "50px", // Taille réduite
                    height: "50px", // Taille réduite
                    "@media (min-width: 1024px)": {
                        display: "none", // Masque l'icône en plus grand
                    },
                })} />
            </div>
            <SubTitle>Créativité</SubTitle>
            <ParagraphStart>
                Nous encourageons l'innovation et cherchons constamment de nouvelles idées pour vous aider à résoudre vos défis les plus complexes.
            </ParagraphStart>
        </div>

        <!-- Icône Ampoule dans un cercle (masquée sous 1024px) -->
        <div class={css({
            display: "none", // Masque le cercle en dessous de 1024px
            justifyContent: "center",
            alignItems: "center",
            "@media (min-width: 1024px)": {
                display: "flex",
                gridColumn: "2 / 5", // Colonne 4
                gridRow: "1 / 2",    // Ligne 1
            },
        })}>
            <div class={css({
                borderRadius: "50%",
                backgroundColor: "white",
                width: "250px",
                height: "250px",
                display: "flex",
                justifyContent: "center",
                alignItems: "center",
                border: "5px solid secondary.100",
            })}>
                <div class={css({
                    borderRadius: "50%",
                    width: "240px",
                    height: "240px",
                    border: "5px solid #ed7d1a",
                    display: "flex",
                    justifyContent: "center",
                    alignItems: "center",
                })}>
                    <img src="/img/icons/ampoule.svg" alt="Ampoule" class={css({
                        width: "100px",
                        height: "100px",
                    })} />
                </div>
            </div>
        </div>

        <!-- Bloc pour Simplicité -->
        <div class={css({
            display: "flex",
            flexDirection: "column",
            justifyContent: "center",
            color: "white",
            "@media (min-width: 1024px)": {
                gridColumn: "4 / 5", // Positionne le bloc dans la colonne 4
                gridRow: "2 / 3",     // Positionne le bloc dans la ligne 2
            },
        })}>
            <!-- Icône Finger en dessous de Simplicité -->
            <div class={css({
                display: "flex",
                justifyContent: "center",
                alignItems: "center",
                "@media (max-width: 1024px)": {
                    marginBottom: "1rem", // Espacement entre l'icône et le sous-titre
                },
            })}>
                <img src="/img/icons/finger.svg" alt="Finger" class={css({
                    width: "50px", // Taille réduite
                    height: "50px", // Taille réduite
                    "@media (min-width: 1024px)": {
                        display: "none", // Masque l'icône en plus grand
                    },
                })} />
            </div>
            <SubTitle>Simplicité</SubTitle>
            <ParagraphStart class={css({ color: "white" })}>
                Nos processus sont conçus pour être intuitifs et faciles à suivre, vous permettant de bénéficier pleinement et simplement de nos solutions complètes, depuis la conception jusqu'à la livraison.
            </ParagraphStart>
        </div>

        <!-- Icône Finger dans un cercle (masquée sous 1024px) -->
        <div class={css({
            display: "none", // Masque le cercle en dessous de 1024px
            justifyContent: "center",
            alignItems: "center",
            "@media (min-width: 1024px)": {
                display: "flex",
                gridColumn: "3 / 4", // Colonne 3
                gridRow: "2 / 3",    // Ligne 2
            },
        })}>
            <div class={css({
                borderRadius: "50%",
                backgroundColor: "white",
                width: "250px",
                height: "250px",
                display: "flex",
                justifyContent: "center",
                alignItems: "center",
                border: "5px solid secondary.100",
            })}>
                <div class={css({
                    borderRadius: "50%",
                    width: "240px",
                    height: "240px",
                    border: "5px solid #ed7d1a",
                    display: "flex",
                    justifyContent: "center",
                    alignItems: "center",
                })}>
                    <img src="/img/icons/finger.svg" alt="Finger" class={css({
                        width: "100px",
                        height: "100px",
                    })} />
                </div>
            </div>
        </div>

    </div>
</section>
