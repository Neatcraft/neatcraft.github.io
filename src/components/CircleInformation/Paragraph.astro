---
import {paragraph} from "../../../styled-system/patterns";

const {extendClass} = Astro.props

---

<p class={paragraph({
    fontSize: {xs: "1rem", xl: "1.2rem"},
    color: "white",
}) + " " + extendClass}>
    <slot/>
</p>
