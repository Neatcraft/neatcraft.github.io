---
import {css} from "../../../styled-system/css";

const {iconSrc, iconAlt} = Astro.props;

---
<img src={iconSrc}
     alt={iconAlt}
     class={css({
         height: {xs:"6vh", lg: "12vh"},
         width: {xs:"6vh", lg: "12vh"},
         marginBottom: "4%"
     })}
/>